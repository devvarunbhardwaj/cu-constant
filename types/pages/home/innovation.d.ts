// Generated by the CU CMS. Do not edit.

export type InnovationTone = 'ink' | 'red' | 'track' | 'plain';

export type GlyphKey = 'lattice' | 'orbit' | 'ascent' | 'broadcast';

export type EcosystemStat = {
  id: string;
  /** Display string, parsed by `parseFigure` so the number tweens. */
  value: string;
  /** Rendered ahead of the tweening number, so a currency mark is never swept into the count. */
  prefix: string;
  label: string;
  tone: InnovationTone;
};

export type IncubationPillar = {
  id: string;
  title: string;
  body: string;
  chips: string[];
  glyph: GlyphKey;
};

export type FoundedStartup = {
  id: string;
  name: string;
  sector: string;
  founders: string;
  description: string;
  /** Where the background clip was cut from; the desktop "Watch their story" link. */
  videoUrl: string;
  /** Muted 15s loop that plays behind the card while it is in view. */
  clipSrc: string;
  /** First frame of `clipSrc`; stands in before the clip loads and under reduced motion. */
  clipPoster: string;
};

export type InnovationGridImage = {
  src: string;
  alt: string;
};

export type InnovationGridTile = {
  id: string;
  /** Cell cover, shown in the grid. Always the gallery's first frame. */
  cover: string;
  /** Burned into the cover's bottom-left, over a scrim. */
  caption: string;
  alt: string;
  /** Opened in the lightbox. */
  gallery: InnovationGridImage[];
};

export type InnovationStartupsSection = {
  /** Accent-marked headline — see `parseAccentHeading`. */
  ecosystemHeading: string;
  ecosystemSubheading: string;
  pillarsHeading: string;
  pillarsSubheading: string;
  startupsSubheading: string;
  stats: EcosystemStat[];
  pillars: IncubationPillar[];
  startups: FoundedStartup[];
  tiles: InnovationGridTile[];
};
