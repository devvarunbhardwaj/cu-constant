// Generated by the CU CMS. Do not edit.

import type { CmsImage } from '../../media';

export type TestimonialReel = {
  id: string;
  /** Overlay caption, drawn bottom-left while the clip is paused. */
  title: string;
  videoLink: string;
  image: CmsImage;
};

export type AchieverVideo = {
  id: string;
  /** Named `videoTitle` because `CucetAchieversCarousel` reads that key. */
  videoTitle: string;
  videoLink: string;
};

export type TestimonialSection = {
  /** Accent-marked headline — see `parseAccentHeading`. */
  heading: string;
  testimonials: TestimonialReel[];
  /** Accent-marked headline over the auto-scrolling strip. */
  achieversHeading: string;
  achievers: AchieverVideo[];
};
